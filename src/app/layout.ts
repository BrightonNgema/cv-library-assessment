import '../styles/globals.css';

export const metadata = {
    title: "Job Search - Find 132,840 UK jobs on CV-Library",
    description: "Start your job search with 132,840 live UK vacancies on award-winning CV-Library. Register your CV and find local jobs near you today!",
};

export default function RootLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children
}